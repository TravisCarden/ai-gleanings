# Acme Service

A simple HTTP service for processing widgets.

## Quick Start

```bash
npm install
npm start
```

The service listens on port 3000. It calls the widget API at `https://api.acme.com/v2/widgets`.

For full setup options see [docs/setup.md](docs/setup.md).
