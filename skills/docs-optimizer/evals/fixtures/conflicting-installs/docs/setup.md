# Setup

## Install dependencies

We use yarn for dependency management:

```bash
yarn install
```

## Configure

Set the widget API endpoint:

```bash
export WIDGET_API=https://api-old.acme.com/widgets
```

## Run

```bash
yarn start
```
