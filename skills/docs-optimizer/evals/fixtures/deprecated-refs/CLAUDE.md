# Agent Instructions

To build the project, run `bash scripts/build.sh`.
To deploy, run `bash scripts/deploy.sh`.
To run tests, run `bash scripts/test.sh`.

The agents directory is at `agents/`. New agents should be added there.
